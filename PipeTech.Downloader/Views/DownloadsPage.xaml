<Page
    x:Class="PipeTech.Downloader.Views.DownloadsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:models="using:PipeTech.Downloader.Models"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea">
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            x:Uid="Downloads_Title"
            Style="{StaticResource PageTitleStyle}" />
        <controls:DataGrid
            Grid.Row="1"          
            x:Name="grid"
            IsReadOnly="True"
            AutoGenerateColumns="False"
            RowDetailsVisibilityMode="VisibleWhenSelected"
            GridLinesVisibility="Horizontal"
            ItemsSource="{x:Bind ViewModel.Source, Mode=OneWay}">            
            <controls:DataGrid.Resources>
                <Color x:Key="DataGridRowSelectedBackgroundColor">Transparent</Color>
                <Color x:Key="DataGridRowSelectedHoveredUnfocusedBackgroundColor">Transparent</Color>
                <Color x:Key="DataGridRowSelectedUnfocusedBackgroundColor">Transparent</Color>
                <SolidColorBrush x:Key="DataGridColumnHeaderBackgroundColor" Color="Transparent" />
            </controls:DataGrid.Resources>               
            <controls:DataGrid.Columns>
                <controls:DataGridTemplateColumn Header="" >
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate x:DataType="models:Project">
                            <controls:Expander Header="" IsExpanded="{x:Bind Expanded, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" >
                                <Interactivity:Interaction.Behaviors>
                                    <Core:DataTriggerBehavior Binding="{Binding IsExpanded, Mode=OneWay}"
                                                      ComparisonCondition="Equal"
                                                      Value="False">
                                        <Core:ChangePropertyAction TargetObject="{Binding ElementName=grid}"
                                                                   PropertyName="SelectedItem" 
                                                                   Value="{x:Null}" />
                                    </Core:DataTriggerBehavior>
                                </Interactivity:Interaction.Behaviors>
                            </controls:Expander>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>
                <controls:DataGridTextColumn Binding="{Binding Name, Mode=OneWay}" Header="Project"/>
                <controls:DataGridTextColumn Binding="{Binding DownloadPath, Mode=OneWay}" Header="Download Path" />
                <controls:DataGridTextColumn Binding="{Binding TotalSize, Mode=OneWay}" Header="Size" />
                <controls:DataGridTextColumn Binding="{Binding Progress, Mode=OneWay}" Header="Progress" />
                <controls:DataGridTextColumn Header="Actions" />
                <!--<controls:DataGridTemplateColumn Header="Symbol">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <FontIcon
                                HorizontalAlignment="Left"
                                FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                Glyph="{Binding Symbol}"
                                AutomationProperties.Name="{Binding SymbolName}" />
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>-->
            </controls:DataGrid.Columns>
            <controls:DataGrid.RowDetailsTemplate>
                <DataTemplate x:DataType="models:Project">
                    <Grid>
                        <controls:DataGrid ItemsSource="{x:Bind Inspections}"
                                           IsReadOnly="True"
                                           RowDetailsVisibilityMode="Collapsed"
                                           AutoGenerateColumns="False">
                            <controls:DataGrid.Resources>
                                <SolidColorBrush x:Key="DataGridColumnHeaderBackgroundColor" Color="Transparent" />
                            </controls:DataGrid.Resources>
                            <controls:DataGrid.Columns>
                                <controls:DataGridTextColumn Binding="{Binding Inspection.Name, Mode=OneWay}"
                                                             Header="Inspection" />
                                <controls:DataGridTextColumn Binding="{Binding Inspection.TotalSize, Mode=OneWay}"
                                                             Header="Size" />
                                <controls:DataGridTextColumn Binding="{Binding Inspection.Progress, Mode=OneWay}"
                                                             Header="Progress" />
                                <controls:DataGridTextColumn Header="Actions" />
                            </controls:DataGrid.Columns>
                        </controls:DataGrid>
                    </Grid>
                </DataTemplate>
            </controls:DataGrid.RowDetailsTemplate>
        </controls:DataGrid>
    </Grid>
</Page>
